# GitOverflow Workspace

This is your isolated development environment.

## Resources
Your repositories are cloned into the `resources/` directory.

- The Agent is configured to be aware of all repositories in `resources/`.
- You can add more repositories dynamically using the interface.
